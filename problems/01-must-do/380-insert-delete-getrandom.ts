/**
 * 380. Insert Delete GetRandom O(1) (Medium)
 * Tags: Array, Hash Table, Math, Design, Randomized
 *
 * Implement the RandomizedSet class:
 * - RandomizedSet() Initializes the RandomizedSet object.
 * - insert(val: number): boolean Inserts an item val into the set if not present.
 *   Returns true if the item was not present, false otherwise.
 * - remove(val: number): boolean Removes an item val from the set if present.
 *   Returns true if the item was present, false otherwise.
 * - getRandom(): number Returns a random element from the current set of elements.
 *   Each element must have the same probability of being returned.
 *
 * You must implement the functions of the class such that each function works
 * in average O(1) time complexity.
 *
 * Example:
 * Input: ["RandomizedSet","insert","remove","insert","getRandom","remove","insert","getRandom"]
 *        [[],[1],[2],[2],[],[1],[2],[]]
 * Output: [null,true,false,true,2,true,false,2]
 *
 * Constraints:
 * - -2^31 <= val <= 2^31 - 1
 * - At most 2 * 10^5 calls will be made to insert, remove, and getRandom.
 * - There will be at least one element when getRandom is called.
 */

class RandomizedSet {
  constructor() {
    // TODO
  }

  insert(val: number): boolean {
    // TODO
    return false;
  }

  remove(val: number): boolean {
    // TODO
    return false;
  }

  getRandom(): number {
    // TODO
    return 0;
  }
}
